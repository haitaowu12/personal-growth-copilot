[project]
name = "personal-growth-copilot-skill"
version = "0.4.0a3"
description = "Validation and packaging tools for Personal Growth Copilot"
requires-python = ">=3.11,<3.15"
license = { text = "MIT" }
authors = [{ name = "Tony Wu" }]
dependencies = [
  "jsonschema==4.26.0",
  "PyYAML==6.0.3",
  "rfc3339-validator==0.1.4",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
